package android_questions.landmark_final_round

class RoomDbManager {

    suspend fun addUsers(allUser:List<User>){
        // add the all users to room db
    }

    suspend fun getAllUsers():List<User>{
        // add the all users to room db
        return emptyList()
    }
}